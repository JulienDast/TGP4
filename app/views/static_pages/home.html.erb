<div class="jumbotron" style="background-image: url('https://img.freepik.com/vecteurs-libre/motif-demi-teintes-carre-aleatoire_1409-1062.jpg?w=2000');">
        <div class="text-center">
        <br>
          <h1 class="text-center" style="color: violet">Bienvenue sur The Gossip project !</h1>
          <br>
          <a href="<%= new_gossip_path %>" class="btn btn-primary">Créer un potin</a>
          <br><br>
        </div>
      </div>
<br><br>
<% if session[:user_id].present? %>
  <h2>Bienvenue <%= User.find_by(id: session[:user_id]).first_name %> !</h2>
<% end %>
<br><br>
<h2> Voici les gossips:</h2>
<br>

<table class="table">
  <% @gossip.each do |gossip| %>
    <tr>
      <td class="text-center"><%= link_to gossip.user.first_name, "/user/#{gossip.user.id}" %></td>
      <td class="text-center"><%= link_to gossip.title, "/gossip/#{gossip.id}" %></td>
    </tr>
  <% end %>
</table>

